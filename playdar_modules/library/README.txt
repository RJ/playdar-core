A fuzzy-searchable database of files available on your local machine.

In priv/ is an erlang port for a taglib reader

To scan, something like this:
 library:scan(resolver:resolver_pid(library), "/path/to/mp3"). 


